"""6.G Unit 9 trap — Polygons on the coordinate plane.
Watch-out (from the lesson):
  Subtract with the absolute value — a "negative distance" means you flipped the subtraction order.
"""
from manim import *  # noqa: F401,F403
from _helpers import TrapDeck


class Lesson6G9Trap(TrapDeck):
    TITLE = "Avoid the trap · Polygons on the coordinate plane"
    DOMAIN = "6.G"
    WRONG = "Subtract with the absolute value"
    RIGHT = "a \"negative distance\" means you flipped the subtraction order."
