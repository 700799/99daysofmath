"""6.RP Unit 1 trap — What is a ratio?.
Watch-out (from the lesson):
  Keep the order asked for: 'apples to oranges' puts the apples number first.
"""
from manim import *  # noqa: F401,F403
from _helpers import TrapDeck


class Lesson6RP1Trap(TrapDeck):
    TITLE = "Avoid the trap · What is a ratio?"
    DOMAIN = "6.RP"
    WRONG = "Keep the order asked for"
    RIGHT = "'apples to oranges' puts the apples number first."
