"""6.EE Unit 7 trap — Parts of expressions.
Watch-out (from the lesson):
  Always SIMPLIFY first before counting parts — hidden like-terms can fool you.
"""
from manim import *  # noqa: F401,F403
from _helpers import TrapDeck


class Lesson6EE7Trap(TrapDeck):
    TITLE = "Avoid the trap · Parts of expressions"
    DOMAIN = "6.EE"
    WRONG = "Always SIMPLIFY first before counting parts"
    RIGHT = "hidden like-terms can fool you."
