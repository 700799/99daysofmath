"""6.NS Unit 2 trap — Multiplying & dividing decimals.
Watch-out (from the lesson):
  Count the decimal places in BOTH factors when multiplying.
"""
from manim import *  # noqa: F401,F403
from _helpers import TrapDeck


class Lesson6NS2Trap(TrapDeck):
    TITLE = "Avoid the trap · Multiplying & dividing decimals"
    DOMAIN = "6.NS"
    WRONG = "Count the decimal places in BOTH factors when multiplying."
    RIGHT = "Slow down and re-read."
