"""6.RP Unit 2 trap — Unit rates.
Watch-out (from the lesson):
  'Per' means divide by that quantity — cost per muffin divides money by muffins.
"""
from manim import *  # noqa: F401,F403
from _helpers import TrapDeck


class Lesson6RP2Trap(TrapDeck):
    TITLE = "Avoid the trap · Unit rates"
    DOMAIN = "6.RP"
    WRONG = "'Per' means divide by that quantity"
    RIGHT = "cost per muffin divides money by muffins."
