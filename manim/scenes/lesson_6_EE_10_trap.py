"""6.EE Unit 10 trap — Tables & relationships.
Watch-out (from the lesson):
  Match each x to its matching y row — don't swap them when reading the table.
"""
from manim import *  # noqa: F401,F403
from _helpers import TrapDeck


class Lesson6EE10Trap(TrapDeck):
    TITLE = "Avoid the trap · Tables & relationships"
    DOMAIN = "6.EE"
    WRONG = "Match each x to its matching y row"
    RIGHT = "don't swap them when reading the table."
