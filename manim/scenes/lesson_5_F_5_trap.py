"""5.F Unit 5 trap — Measurement, conversions & volume.
Watch-out (from the lesson):
  Multiply or divide? Going to a SMALLER unit means MORE of them — multiply.
"""
from manim import *  # noqa: F401,F403
from _helpers import TrapDeck


class Lesson5F5Trap(TrapDeck):
    TITLE = "Avoid the trap - Measurement, conversions & volume"
    WRONG = "Multiply or divide? Going to a SMALLER unit means MORE of them"
    RIGHT = "multiply."
