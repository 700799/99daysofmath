"""6.NS Unit 1 trap — Adding & subtracting decimals.
Watch-out (from the lesson):
  Line up the decimal points — not just the last digits.
"""
from manim import *  # noqa: F401,F403
from _helpers import TrapDeck


class Lesson6NS1Trap(TrapDeck):
    TITLE = "Avoid the trap · Adding & subtracting decimals"
    DOMAIN = "6.NS"
    WRONG = "Line up the decimal points"
    RIGHT = "not just the last digits."
