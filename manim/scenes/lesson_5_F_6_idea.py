"""5.F Unit 6 idea — Coordinate plane, patterns & line plots.
Concept bullets (from the lesson plan):
  1. A point is (x, y): go ACROSS x first, then UP y. "Run before you jump."
  2. A pattern with a constant jump: term n = start + jump × (n − 1).
  3. On a line plot, every X is one data value — count Xs to answer questions.
"""
from manim import *  # noqa: F401,F403
from _helpers import IdeaDeck


class Lesson5F6Idea(IdeaDeck):
    TITLE = "The idea - Coordinate plane, patterns & line plots"
    BULLETS = ["A point is (x, y): go ACROSS x first, then UP y. \"Run before you jump.\"", "A pattern with a constant jump: term n = start + jump x (n - 1).", "On a line plot, every X is one data value - count Xs to answer questions."]
