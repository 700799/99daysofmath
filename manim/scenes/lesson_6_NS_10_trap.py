"""6.NS Unit 10 trap — Coordinate plane: distance & polygons.
Watch-out (from the lesson):
  Subtract carefully when one coordinate is negative: 5 − (−2) = 7, not 3.
"""
from manim import *  # noqa: F401,F403
from _helpers import TrapDeck


class Lesson6NS10Trap(TrapDeck):
    TITLE = "Avoid the trap · Coordinate plane: distance & polygons"
    DOMAIN = "6.NS"
    WRONG = "Subtract carefully when one coordinate is negative"
    RIGHT = "5 − (−2) = 7, not 3."
