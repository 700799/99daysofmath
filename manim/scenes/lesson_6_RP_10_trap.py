"""6.RP Unit 10 trap — Ratio & proportion problem-solving.
Watch-out (from the lesson):
  Scale BOTH parts of the ratio by the same factor — never just one side.
"""
from manim import *  # noqa: F401,F403
from _helpers import TrapDeck


class Lesson6RP10Trap(TrapDeck):
    TITLE = "Avoid the trap · Ratio & proportion problem-solving"
    DOMAIN = "6.RP"
    WRONG = "Scale BOTH parts of the ratio by the same factor"
    RIGHT = "never just one side."
