"""6.SP Unit 8 trap — Center: mean & median in depth.
Watch-out (from the lesson):
  Sort the data FIRST when finding the median — order is everything.
"""
from manim import *  # noqa: F401,F403
from _helpers import TrapDeck


class Lesson6SP8Trap(TrapDeck):
    TITLE = "Avoid the trap - Center: mean & median in depth"
    WRONG = "Sort the data FIRST when finding the median"
    RIGHT = "order is everything."
