"""6.G Unit 10 trap — Surface area & nets.
Watch-out (from the lesson):
  Surface area uses SQUARE units. Volume uses cubic — don't mix them up.
"""
from manim import *  # noqa: F401,F403
from _helpers import TrapDeck


class Lesson6G10Trap(TrapDeck):
    TITLE = "Avoid the trap - Surface area & nets"
    WRONG = "Surface area uses SQUARE units. Volume uses cubic"
    RIGHT = "don't mix them up."
