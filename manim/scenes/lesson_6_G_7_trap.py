"""6.G Unit 7 trap — Composite figures (area).
Watch-out (from the lesson):
  For cutouts, SUBTRACT — for additions (like a triangular roof), ADD.
"""
from manim import *  # noqa: F401,F403
from _helpers import TrapDeck


class Lesson6G7Trap(TrapDeck):
    TITLE = "Avoid the trap - Composite figures (area)"
    WRONG = "For cutouts, SUBTRACT"
    RIGHT = "for additions (like a triangular roof), ADD."
