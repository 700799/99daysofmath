"""6.EE Unit 8 trap — Equivalent expressions & checking solutions.
Watch-out (from the lesson):
  Two expressions can MATCH at one value but still differ — use the algebra to be sure.
"""
from manim import *  # noqa: F401,F403
from _helpers import TrapDeck


class Lesson6EE8Trap(TrapDeck):
    TITLE = "Avoid the trap · Equivalent expressions & checking solutions"
    DOMAIN = "6.EE"
    WRONG = "Two expressions can MATCH at one value but still differ"
    RIGHT = "use the algebra to be sure."
