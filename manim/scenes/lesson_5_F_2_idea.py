"""5.F Unit 2 idea — Adding & subtracting fractions.
Concept bullets (from the lesson plan):
  1. You can only add or subtract pieces that are the SAME size — same denominator.
  2. Rewrite each fraction using a common denominator first (often the LCM).
  3. Then add or subtract just the numerators and simplify.
"""
from manim import *  # noqa: F401,F403
from _helpers import IdeaDeck


class Lesson5F2Idea(IdeaDeck):
    TITLE = "The idea - Adding & subtracting fractions"
    BULLETS = ["You can only add or subtract pieces that are the SAME size - same denominator.", "Rewrite each fraction using a common denominator first (often the LCM).", "Then add or subtract just the numerators and simplify."]
